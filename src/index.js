import React from 'react';
import ReactDOM from 'react-dom/client';
import PropertyManager from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <PropertyManager />
  </React.StrictMode>
);